import { Button, styled, Typography } from '@mui/material'

export const ActionButtonGroupHeader = styled(Button)({
  cursor: 'default'
})

export const ActionButtonToolTip = styled(Typography)({
  cursor: 'default',
  fontSize: '0.6rem'
})
